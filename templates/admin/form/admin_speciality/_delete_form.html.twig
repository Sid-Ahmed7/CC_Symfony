<form method="post" action="{{ path('app_admin_speciality_delete', {'id': speciality.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément
?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ speciality.id) }}">
    <button class="btn">Supprimer</button>
</form>
