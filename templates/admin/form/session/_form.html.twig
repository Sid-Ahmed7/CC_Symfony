<div class="max-w-4xl mx-auto p-6 bg-white shadow-lg rounded-lg border border-gray-200">
    {{ form_start(form) }}
        <div class="mb-6">
            {{ form_label(form.title, 'Titre de la session', {'label_class': 'text-sm font-semibold text-gray-700'}) }}
            <div class="mt-2">
                {{ form_widget(form.title, {
                    'attr': {'class': 'w-full px-4 py-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300 ease-in-out'}
                }) }}
            </div>
            {{ form_errors(form.title) }}
        </div>

        <div class="mb-6">
            {{ form_label(form.description, 'Description', {'label_class': 'text-sm font-semibold text-gray-700'}) }}
            <div class="mt-2">
                {{ form_widget(form.description, {
                    'attr': {'class': 'w-full px-4 py-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300 ease-in-out'}
                }) }}
            </div>
            {{ form_errors(form.description) }}
        </div>

        <div class="mb-6">
            {{ form_label(form.date, 'Date de la session', {'label_class': 'text-sm font-semibold text-gray-700'}) }}
            <div class="mt-2">
                {{ form_widget(form.date, {
                    'attr': {'class': 'w-full px-4 py-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300 ease-in-out'}
                }) }}
            </div>
            {{ form_errors(form.date) }}
        </div>

        <div class="mb-6">
            {{ form_label(form.duration, 'Durée (en minutes)', {'label_class': 'text-sm font-semibold text-gray-700'}) }}
            <div class="mt-2">
                {{ form_widget(form.duration, {
                    'attr': {'class': 'w-full px-4 py-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300 ease-in-out'}
                }) }}
            </div>
            {{ form_errors(form.duration) }}
        </div>

        <div class="mb-6">
            {{ form_label(form.location, 'Lieu', {'label_class': 'text-sm font-semibold text-gray-700'}) }}
            <div class="mt-2">
                {{ form_widget(form.location, {
                    'attr': {'class': 'w-full px-4 py-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300 ease-in-out'}
                }) }}
            </div>
            {{ form_errors(form.location) }}
        </div>

        <div class="mb-6">
            {{ form_label(form.status, 'Statut', {'label_class': 'text-sm font-semibold text-gray-700'}) }}
            <div class="mt-2">
                {{ form_widget(form.status, {
                    'attr': {'class': 'w-full px-4 py-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300 ease-in-out'}
                }) }}
            </div>
            {{ form_errors(form.status) }}
        </div>

        <!-- Program Field -->
        <div class="mb-6">
            {{ form_label(form.program, 'Programme', {'label_class': 'text-sm font-semibold text-gray-700'}) }}
            <div class="mt-2">
                {{ form_widget(form.program, {
                    'attr': {'class': 'w-full px-4 py-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300 ease-in-out'}
                }) }}
            </div>
            {{ form_errors(form.program) }}
        </div>

        <!-- Coach Field -->
        <div class="mb-6">
            {{ form_label(form.coach, 'Coach', {'label_class': 'text-sm font-semibold text-gray-700'}) }}
            <div class="mt-2">
                {{ form_widget(form.coach, {
                    'attr': {'class': 'w-full px-4 py-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300 ease-in-out'}
                }) }}
            </div>
            {{ form_errors(form.coach) }}
        </div>

        <div class="mb-6 text-right">
            <button type="submit" class="bg-blue-500 text-white py-3 px-8 rounded-md hover:bg-blue-600 transition duration-300 ease-in-out focus:outline-none focus:ring-2 focus:ring-blue-300">
                Enregistrer
            </button>
        </div>
    {{ form_end(form) }}
</div>
