<form method="post" action="{{ path('app_session_history_delete', {'id': sessionH.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément
?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sessionH.id) }}">
    <button class="btn">Supprimer</button>
</form>
