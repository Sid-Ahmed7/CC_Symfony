{{ form_start(form) }}
    <div class="max-w-lg mx-auto bg-white p-6 rounded-lg shadow-lg space-y-4">
        <h2 class="text-2xl font-semibold text-gray-800 mb-6">Ajouter un Suivi de Séance</h2>


        <!-- Goals -->
        <div class="form-group">
            <label for="{{ form.goals.vars.id }}" class="block text-gray-700 text-sm font-semibold mb-2">Objectifs</label>
            {{ form_row(form.goals, {'attr': {'class': 'w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500'}}) }}
        </div>

        <!-- Comments -->
        <div class="form-group">
            <label for="{{ form.comments.vars.id }}" class="block text-gray-700 text-sm font-semibold mb-2">Commentaires</label>
            {{ form_row(form.comments, {'attr': {'class': 'w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500'}}) }}
        </div>


        <!-- Submit Button -->
        <button type="submit" class="w-full bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600 transition duration-300">Enregistrer</button>
    </div>
{{ form_end(form) }}
