<form method="post" action="{{ path('app_admin_delete_coach', {'id': coach.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coach.id) }}">
    <button class=" btn bg-red-500 text-white py-2 px-4 rounded-lg hover:bg-red-600 transition-colors duration-300">Delete</button>
</form>
