    

        {{ form_start(form) }}
        
        <div class="mb-6">
            {{ form_label(form.name, null, {'label_attr': {'class': 'block text-gray-700 font-medium'}}) }}
            {{ form_widget(form.name, {'attr': {'class': 'w-full p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-600'}}) }}
            {{ form_errors(form.name) }}
        </div>

        <div class="text-center">
            <button type="submit" class="bg-blue-500 text-white py-2 px-6 rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">
                Enregistrer la catégorie
            </button>
        </div>

        {{ form_end(form) }}
   

